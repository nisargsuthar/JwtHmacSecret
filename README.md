# Brute force HMAC secrets for JWT tokens.

## Usage

Edit JWT details in the script & provide the wordlist.
```python3
python JwtHmacSecret.py
```

If no matches found in the wordlist, the prompt will return.